public class Debugger {
    // Explain what the following code does: ((n & (n-1)) == 0
    // They have no 1s in the same place because n-1 is the exact opposite of n in terms of binary 1s and 0s
}
